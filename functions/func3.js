// a write a function that takes two numbers multiply them and divide them by it addition
function multiAdd(num1, num2) {
  return (num1 * num2) / (num1 + num2);
}

let n = multiAdd(5, 10);
console.log(n);
