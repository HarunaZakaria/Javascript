// merge multiple arrays together
const names = ['Haruna', 'Cury', 'Aziz', 'Dauda', 'Hamis'];
const colors =['red', 'green', 'blue', 'yellow'];
const combine = names.concat(colors);
console.log(combine[4]);
