//array of names
const names = ['Haruna', 'Cury', 'Aziz', 'Dauda', 'Hamis'];
/*
the function causes each item to be logged to the console, 
along with the item's index number.
*/
names.forEach((item, index, array) => {
    console.log( item, index);

});
