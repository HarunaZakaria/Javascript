const names = ['Haruna', 'Soaliho', 'Dauda', 'Napari', 'Hamis', 'Aisha'];
// out each name with it corresponding idex
names.forEach((name, index) => {
  console.log(`at ${index} the name is ${name}`);
});
